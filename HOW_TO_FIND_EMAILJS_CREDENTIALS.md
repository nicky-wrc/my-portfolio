# วิธีหา EmailJS Credentials (Service ID, Template ID, Public Key)

## 📍 ดูค่าจาก EmailJS Dashboard

### ขั้นตอนที่ 1: Login เข้า EmailJS
1. ไปที่ **https://www.emailjs.com/**
2. คลิก **Login** (มุมขวาบน)
3. Login ด้วย account ของคุณ

---

## 🔑 1. Service ID (NEXT_PUBLIC_EMAILJS_SERVICE_ID)

### วิธีหา:
1. หลังจาก login แล้ว → ไปที่ **Dashboard**
2. คลิก **Email Services** (เมนูด้านซ้าย)
3. คุณจะเห็น Service ที่สร้างไว้ (เช่น "Portfolio Contact", "Gmail", ฯลฯ)
4. คลิกเข้าไปที่ Service ที่คุณใช้
5. **Service ID** จะแสดงอยู่ด้านบน หรือในส่วน **Service Details**
   - ตัวอย่าง: `service_abc123` หรือ `service_xyz789`

### ตัวอย่าง:
```
Service ID: service_abc123
```

---

## 📝 2. Template ID (NEXT_PUBLIC_EMAILJS_TEMPLATE_ID)

### วิธีหา:
1. ไปที่ **Email Templates** (เมนูด้านซ้าย)
2. คุณจะเห็น Template ที่สร้างไว้ (เช่น "Contact Form", "Portfolio Contact", ฯลฯ)
3. คลิกเข้าไปที่ Template ที่คุณใช้
4. **Template ID** จะแสดงอยู่ด้านบน หรือในส่วน **Template Details**
   - ตัวอย่าง: `template_abc123` หรือ `template_xyz789`

### ตัวอย่าง:
```
Template ID: template_abc123
```

---

## 🔐 3. Public Key (NEXT_PUBLIC_EMAILJS_PUBLIC_KEY)

### วิธีหา:
1. ไปที่ **Account** (เมนูด้านซ้าย หรือคลิกที่ profile icon)
2. คลิก **General** (หรือ **Account Settings**)
3. หาส่วน **Public Key** หรือ **API Keys**
4. **Public Key** จะแสดงอยู่ที่นั่น
   - ตัวอย่าง: `abc123xyz789` หรือ `xxxxxxxxxxxxx`

### ตัวอย่าง:
```
Public Key: abc123xyz789abcdef
```

---

## 📋 สรุปค่าที่ต้องใช้

หลังจากหาค่าทั้ง 3 ตัวแล้ว คุณจะได้:

```env
NEXT_PUBLIC_EMAILJS_SERVICE_ID=service_abc123
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=template_abc123
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=abc123xyz789abcdef
```

---

## ✅ วิธีตรวจสอบว่าค่าถูกต้องหรือไม่

### ตรวจสอบใน EmailJS Dashboard:
1. **Service ID**: ควรขึ้นต้นด้วย `service_`
2. **Template ID**: ควรขึ้นต้นด้วย `template_`
3. **Public Key**: เป็น string ยาวๆ ไม่มี prefix

### ตัวอย่างรูปแบบที่ถูกต้อง:
- ✅ Service ID: `service_abc123`
- ✅ Template ID: `template_xyz789`
- ✅ Public Key: `abc123xyz789abcdef`

### ตัวอย่างรูปแบบที่ผิด:
- ❌ Service ID: `abc123` (ขาด `service_`)
- ❌ Template ID: `xyz789` (ขาด `template_`)
- ❌ Public Key: `public_key_abc123` (ไม่ควรมี prefix)

---

## 🔄 ถ้ายังไม่มี Service หรือ Template

### สร้าง Service ใหม่:
1. ไปที่ **Email Services** → **Add New Service**
2. เลือก email provider (เช่น Gmail)
3. ใส่ email: `nick.worachatz@gmail.com`
4. ตั้งชื่อ service
5. คัดลอก **Service ID** ที่ได้

### สร้าง Template ใหม่:
1. ไปที่ **Email Templates** → **Create New Template**
2. ตั้งชื่อ template
3. ตั้งค่า Content และ Settings
4. คัดลอก **Template ID** ที่ได้

---

## 📝 หมายเหตุ

- **Service ID** และ **Template ID** จะมี prefix (`service_` และ `template_`) อยู่แล้ว
- **Public Key** ไม่มี prefix
- ค่าทั้ง 3 ตัวนี้สามารถเปิดเผยได้ (ใช้ใน client-side) แต่ไม่ควรเปิดเผย Private Key
- ถ้าลืมค่าอะไร สามารถกลับไปดูใน EmailJS Dashboard ได้ตลอด

---

## 🆘 ถ้าหาไม่เจอ

1. **ตรวจสอบว่า Login แล้วหรือยัง**
   - ต้อง login เข้า EmailJS Dashboard ก่อน

2. **ตรวจสอบว่า Service/Template ถูกสร้างแล้วหรือยัง**
   - ถ้ายังไม่มี ต้องสร้างก่อน

3. **ลองดูในส่วนอื่น**
   - บางครั้งอาจอยู่ในส่วน Settings หรือ Details

4. **Contact EmailJS Support**
   - ถ้ายังหาไม่เจอ ลองติดต่อ support@emailjs.com

---

## 🔗 ลิงก์ที่เกี่ยวข้อง

- EmailJS Dashboard: https://www.emailjs.com/
- EmailJS Documentation: https://www.emailjs.com/docs/

